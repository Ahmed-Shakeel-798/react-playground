import React from "react";
import ReactDOM from "react-dom";

import App from "./components/App";
/**
 * Here we learn about the react components
 * A component is a function that rfeturns some HTML
 * It has to be in Pascal case
 * Look at the components that we created in ./Heading and ./List files
 */

ReactDOM.render(<div><App /></div>, document.getElementById("root"));
